<header class="page-header">
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href='/'>
        <i data-feather="home"></i>
    </a>
    <!-- 
        navbar-toggler buttons start out hidden with display:none
        For a small screen they become visible, containing a hamburger icon,
        and the .navbar-collapse is set to display:none.  

        The effect is to instantiate a popup menu matching the
        buttons in the navbar.

        A navbar-toggler immediately to the right of
        a navbar-brand gets pushed to the far right, otherwise
        it pushes to the far left
    -->
    <button class="navbar-toggler" type="button" 
        data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" 
        aria-expanded="false" 
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <span class="navbar-text text-dark col">{{ title }}</span>
        <a class="nav-item nav-link btn btn-dark col-auto" href='/notes/add'>ADD Note</a>
    </div>
</nav>
</header>
